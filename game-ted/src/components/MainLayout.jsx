import React from 'react';
import NavBar from './NavBar'; // Barra de navegação padrão

const MainLayout = () => {
  return (
    <div>
      <NavBar />
    </div>
  );
};

export default MainLayout;
